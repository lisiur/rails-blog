<p id="notice"></p>


<table class="table table-hover">
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @admin_archives.each do |admin_archive| %>
      <tr>
        <td><%= link_to "#{admin_archive.year}/#{admin_archive.month}","/admin/archive/#{admin_archive.year}/#{admin_archive.month}" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
